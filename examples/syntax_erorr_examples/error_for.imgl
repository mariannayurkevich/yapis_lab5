proc test1() {
    for int i = 0 i < 10 i = i + 1 {  // Ошибка: пропущены скобки и точки с запятой в for
        write(i)
    }                                   // Ошибка: пропущена точка с запятой после write(i)
    
    if true then {                      // Ошибка: нет скобок вокруг условия
        write("true")
    } else {
        write("false")
    ;
}